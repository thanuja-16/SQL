Day - 5

12|8|2026(5 days)

create table new_table like old_table;
create table employee like emp;;

insert into employee select * from emp;
insert into new_table select * from existing_table;

auto_increment:
it is a feature of MySQL it is automatically generate the number when we are insert the new row/record.

create table student(id int primary key auto_increament,
                     name varchar(20),age int
);
 insert into student(name,age) values
  ('sam',20), ('ram',32);


for retrive only few columns in a table
select name, age from student;

where cluse 
where cluse is used to filter the records or rows in the tables based on conditions.

operators: the operators is a symbol that used do operands
Arithematic 
these are used to do mathematical operations
(+,-,*,/,%)
select name, age, age+10 from student;

relational/comparision operation:
=,>,<,>=,<=,!= or <>

logical operators it is used to compare more than one
and, or, not

bit wise operators:
&,|,^,<<,>>

select * from students where age>=30;
select name, age from students where
age=25 or age=60
select * from students where age!=25;

alias names:
alias names is used to customize the from columns .
as----alias names
select name, age, age-10 as sub from students;
